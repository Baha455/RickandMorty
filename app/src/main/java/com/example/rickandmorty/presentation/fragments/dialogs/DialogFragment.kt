package com.example.rickandmorty.presentation.fragments.dialogs

import android.app.AlertDialog
import android.app.Dialog
import android.os.Bundle
import android.os.Parcel
import android.os.Parcelable
import android.view.LayoutInflater
import android.view.View
import androidx.appcompat.app.AppCompatDialogFragment
import com.example.rickandmorty.databinding.FilterDialogBinding

open class DialogFragment() : AppCompatDialogFragment() {

    //private lateinit var binding: FilterDialogBinding

/*
    override fun onCreateDialog(savedInstanceState: Bundle?): Dialog {
        val bind :FilterDialogBinding = FilterDialogBinding .inflate(layoutInflater)
        dialog?.setContentView(bind.root)
    }
}*/
}